import React from 'react';

export default ()=> {
    return(
        <div className="top-bar">
            <h1>Filtering the list</h1>
            <img className="logo" src="/static/icons/logo.svg" alt="logo"/>
        </div>
    )   
}